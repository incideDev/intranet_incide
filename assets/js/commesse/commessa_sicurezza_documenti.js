(function () {
  // Se gli elementi "gestione documenti" non esistono, esci SUBITO (no-op)
  var hasUI = document.getElementById('docList') || document.getElementById('docForm');
  if (!hasUI) return;
  // Se in futuro questa pagina avrà lista/modale documenti, reintrodurremo qui la logica.
})();
